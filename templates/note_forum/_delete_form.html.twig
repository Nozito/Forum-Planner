<form method="post" action="{{ path('app_note_forum_delete', {'id': note_forum.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ note_forum.id) }}">
    <button class="btn">Delete</button>
</form>
