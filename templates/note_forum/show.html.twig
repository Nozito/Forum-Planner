{% extends 'base.html.twig' %}

{% block title %}NoteForum{% endblock %}

{% block body %}
    <h1>NoteForum</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ note_forum.id }}</td>
            </tr>
            <tr>
                <th>Note</th>
                <td>{{ note_forum.note }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_note_forum_index') }}">back to list</a>

    <a href="{{ path('app_note_forum_edit', {'id': note_forum.id}) }}">edit</a>

    {{ include('note_forum/_delete_form.html.twig') }}
{% endblock %}
