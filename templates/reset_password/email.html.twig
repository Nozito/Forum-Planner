<h1 class="text-2xl font-bold mb-4">Bonjour 👋 !</h1>

<p class="text-gray-700 mb-4">
    Pour réinitialiser votre mot de passe, cliquez sur le lien ci-dessous :
</p>

<a href="{{ url('app_reset_password', {token: resetToken.token}) }}"
   class="text-blue-500 hover:text-blue-700 underline break-all mb-4 block">
    {{ url('app_reset_password', {token: resetToken.token}) }}
</a>

<p class="text-gray-700 mb-4">
    Ce lien expirera dans
    <span class="font-semibold">{{ resetToken.expirationMessageKey|trans(resetToken.expirationMessageData, 'ResetPasswordBundle') }}</span>.
</p>

<p class="text-gray-700">Bisous 😘!</p>